name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt pytest
      - name: Smoke tests (mock mode)
        run: PYTHONPATH=. python -m pytest -q
      - name: Success-metric evals
        run: PYTHONPATH=. python ../evals/run_evals.py

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - run: npm ci
      - run: npm run build

  # --- Deploy (enable by adding repo secrets) -------------------------------
  # deploy-frontend:
  #   needs: [backend, frontend]
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: npx vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }}
  #
  # deploy-backend:
  #   # Hugging Face Spaces auto-deploys on git push to the Space remote.
  #   # Add HF_TOKEN secret and push backend/ to the Space, or use the HF action.
